diff --git a/app/javascript/dashboard/routes/dashboard/crm/Pipeline.vue b/app/javascript/dashboard/routes/dashboard/crm/Pipeline.vue
index ca26817..aad4f5a 100644
--- a/app/javascript/dashboard/routes/dashboard/crm/Pipeline.vue
+++ b/app/javascript/dashboard/routes/dashboard/crm/Pipeline.vue
@@ -128,19 +128,59 @@ const moneyFormatter = new Intl.NumberFormat('en-AE', {
 
 const dateFormatter = new Intl.DateTimeFormat('en-GB', {
   day: '2-digit',
-  month: 'short',
+  month: '2-digit',
   year: 'numeric',
 });
 
 const dateTimeFormatter = new Intl.DateTimeFormat('en-GB', {
   day: '2-digit',
-  month: 'short',
+  month: '2-digit',
   year: 'numeric',
   hour: '2-digit',
   minute: '2-digit',
   hour12: false,
 });
 
+const DATE_INPUT_PLACEHOLDER = 'dd/mm/yyyy';
+const ISO_DATE_PATTERN = /^(\d{4})-(\d{2})-(\d{2})$/;
+const DISPLAY_DATE_PATTERN = /^(\d{1,2})[./-](\d{1,2})[./-](\d{4})$/;
+
+const padDatePart = value => String(value).padStart(2, '0');
+
+const formatDateInputValue = value => {
+  if (!value) return '';
+
+  const rawValue = String(value).trim();
+  const isoMatch = rawValue.match(ISO_DATE_PATTERN);
+  if (isoMatch) return `${isoMatch[3]}/${isoMatch[2]}/${isoMatch[1]}`;
+
+  return rawValue;
+};
+
+const normalizeDateInputValue = value => {
+  const rawValue = String(value || '').trim();
+  if (!rawValue) return '';
+  if (ISO_DATE_PATTERN.test(rawValue)) return rawValue;
+
+  const displayMatch = rawValue.match(DISPLAY_DATE_PATTERN);
+  if (!displayMatch) return '';
+
+  const day = Number(displayMatch[1]);
+  const month = Number(displayMatch[2]);
+  const year = Number(displayMatch[3]);
+  const date = new Date(Date.UTC(year, month - 1, day));
+
+  if (
+    date.getUTCFullYear() !== year ||
+    date.getUTCMonth() !== month - 1 ||
+    date.getUTCDate() !== day
+  ) {
+    return '';
+  }
+
+  return `${year}-${padDatePart(month)}-${padDatePart(day)}`;
+};
+
 const cloneFilters = filters => ({
   ...buildEmptyFilters(),
   ...(filters || {}),
@@ -611,7 +651,7 @@ const selectOptionsForField = field => {
 };
 
 const inputTypeForField = field => {
-  if (field.field_type === 'date') return 'date';
+  if (field.field_type === 'date') return 'text';
   if (field.field_type === 'number') return 'number';
   return 'text';
 };
@@ -620,6 +660,20 @@ const stepForField = field => {
   return field.field_type === 'number' ? '0.01' : undefined;
 };
 
+const rangeInputValue = (field, value) => {
+  return field.field_type === 'date' ? formatDateInputValue(value) : value || '';
+};
+
+const setDraftRangeFieldFilter = (field, rangePart, value) => {
+  const nextValue = field.field_type === 'date' ? normalizeDateInputValue(value) : value;
+  setDraftFieldFilter(field, { [rangePart]: nextValue });
+};
+
+const handleRangeFieldInput = (field, rangePart, value) => {
+  if (field.field_type === 'date') return;
+  setDraftRangeFieldFilter(field, rangePart, value);
+};
+
 const isTextFilterField = field => {
   return !['select', 'boolean', 'date', 'number'].includes(field.field_type);
 };
@@ -1823,9 +1877,12 @@ watch(
               >
                 Created from
                 <input
-                  v-model="draftFilters.createdFrom"
+                  :value="formatDateInputValue(draftFilters.createdFrom)"
                   class="h-8 rounded-md border border-n-weak bg-n-background px-2 text-xs text-n-slate-12 outline-none focus:border-n-brand"
-                  type="date"
+                  inputmode="numeric"
+                  :placeholder="DATE_INPUT_PLACEHOLDER"
+                  type="text"
+                  @change="draftFilters.createdFrom = normalizeDateInputValue($event.target.value)"
                 />
               </label>
 
@@ -1835,9 +1892,12 @@ watch(
               >
                 Created to
                 <input
-                  v-model="draftFilters.createdTo"
+                  :value="formatDateInputValue(draftFilters.createdTo)"
                   class="h-8 rounded-md border border-n-weak bg-n-background px-2 text-xs text-n-slate-12 outline-none focus:border-n-brand"
-                  type="date"
+                  inputmode="numeric"
+                  :placeholder="DATE_INPUT_PLACEHOLDER"
+                  type="text"
+                  @change="draftFilters.createdTo = normalizeDateInputValue($event.target.value)"
                 />
               </label>
 
@@ -1885,24 +1945,30 @@ watch(
                   <label class="grid gap-1 text-[11px] font-medium text-n-slate-11">
                     {{ field.label }} from
                     <input
+                      :inputmode="field.field_type === 'date' ? 'numeric' : undefined"
                       :min="field.field_type === 'number' ? '0' : undefined"
+                      :placeholder="field.field_type === 'date' ? DATE_INPUT_PLACEHOLDER : undefined"
                       :step="stepForField(field)"
                       :type="inputTypeForField(field)"
-                      :value="fieldFilterState(draftFilters, field).from || ''"
+                      :value="rangeInputValue(field, fieldFilterState(draftFilters, field).from)"
                       class="h-8 rounded-md border border-n-weak bg-n-background px-2 text-xs text-n-slate-12 outline-none focus:border-n-brand"
-                      @input="setDraftFieldFilter(field, { from: $event.target.value })"
+                      @input="handleRangeFieldInput(field, 'from', $event.target.value)"
+                      @change="setDraftRangeFieldFilter(field, 'from', $event.target.value)"
                     />
                   </label>
 
                   <label class="grid gap-1 text-[11px] font-medium text-n-slate-11">
                     {{ field.label }} to
                     <input
+                      :inputmode="field.field_type === 'date' ? 'numeric' : undefined"
                       :min="field.field_type === 'number' ? '0' : undefined"
+                      :placeholder="field.field_type === 'date' ? DATE_INPUT_PLACEHOLDER : undefined"
                       :step="stepForField(field)"
                       :type="inputTypeForField(field)"
-                      :value="fieldFilterState(draftFilters, field).to || ''"
+                      :value="rangeInputValue(field, fieldFilterState(draftFilters, field).to)"
                       class="h-8 rounded-md border border-n-weak bg-n-background px-2 text-xs text-n-slate-12 outline-none focus:border-n-brand"
-                      @input="setDraftFieldFilter(field, { to: $event.target.value })"
+                      @input="handleRangeFieldInput(field, 'to', $event.target.value)"
+                      @change="setDraftRangeFieldFilter(field, 'to', $event.target.value)"
                     />
                   </label>
                 </template>
@@ -2634,17 +2700,23 @@ watch(
               <label class="grid gap-1.5 text-xs font-medium text-n-slate-11">
                 Rental start
                 <input
-                  v-model="newDealForm.rental_start"
+                  :value="formatDateInputValue(newDealForm.rental_start)"
                   class="h-10 rounded-md border border-n-weak bg-n-background px-3 text-sm text-n-slate-12 outline-none focus:border-n-brand"
-                  type="date"
+                  inputmode="numeric"
+                  :placeholder="DATE_INPUT_PLACEHOLDER"
+                  type="text"
+                  @change="newDealForm.rental_start = normalizeDateInputValue($event.target.value)"
                 />
               </label>
               <label class="grid gap-1.5 text-xs font-medium text-n-slate-11">
                 Rental end
                 <input
-                  v-model="newDealForm.rental_end"
+                  :value="formatDateInputValue(newDealForm.rental_end)"
                   class="h-10 rounded-md border border-n-weak bg-n-background px-3 text-sm text-n-slate-12 outline-none focus:border-n-brand"
-                  type="date"
+                  inputmode="numeric"
+                  :placeholder="DATE_INPUT_PLACEHOLDER"
+                  type="text"
+                  @change="newDealForm.rental_end = normalizeDateInputValue($event.target.value)"
                 />
               </label>
             </div>
diff --git a/app/javascript/dashboard/routes/dashboard/crm/DealWorkspace.vue b/app/javascript/dashboard/routes/dashboard/crm/DealWorkspace.vue
index f81c91a..f41e6aa 100644
--- a/app/javascript/dashboard/routes/dashboard/crm/DealWorkspace.vue
+++ b/app/javascript/dashboard/routes/dashboard/crm/DealWorkspace.vue
@@ -62,10 +62,50 @@ const moneyFormatter = new Intl.NumberFormat('en-AE', {
 
 const dateFormatter = new Intl.DateTimeFormat('en-GB', {
   day: '2-digit',
-  month: 'short',
+  month: '2-digit',
   year: 'numeric',
 });
 
+const DATE_INPUT_PLACEHOLDER = 'dd/mm/yyyy';
+const ISO_DATE_PATTERN = /^(\d{4})-(\d{2})-(\d{2})$/;
+const DISPLAY_DATE_PATTERN = /^(\d{1,2})[./-](\d{1,2})[./-](\d{4})$/;
+
+const padDatePart = value => String(value).padStart(2, '0');
+
+const formatDateInputValue = value => {
+  if (!value) return '';
+
+  const rawValue = String(value).trim();
+  const isoMatch = rawValue.match(ISO_DATE_PATTERN);
+  if (isoMatch) return `${isoMatch[3]}/${isoMatch[2]}/${isoMatch[1]}`;
+
+  return rawValue;
+};
+
+const normalizeDateInputValue = value => {
+  const rawValue = String(value || '').trim();
+  if (!rawValue) return '';
+  if (ISO_DATE_PATTERN.test(rawValue)) return rawValue;
+
+  const displayMatch = rawValue.match(DISPLAY_DATE_PATTERN);
+  if (!displayMatch) return '';
+
+  const day = Number(displayMatch[1]);
+  const month = Number(displayMatch[2]);
+  const year = Number(displayMatch[3]);
+  const date = new Date(Date.UTC(year, month - 1, day));
+
+  if (
+    date.getUTCFullYear() !== year ||
+    date.getUTCMonth() !== month - 1 ||
+    date.getUTCDate() !== day
+  ) {
+    return '';
+  }
+
+  return `${year}-${padDatePart(month)}-${padDatePart(day)}`;
+};
+
 const FIELD_GROUP_KEYS = {
   main: ['assigned_manager', 'assigned_manager_id', 'manager', 'client_status', 'language', 'client_type', 'customer_type'],
   car: ['car_model', 'car_type', 'requested_car', 'selected_car'],
@@ -467,7 +507,7 @@ const isCustomField = field => {
 };
 
 const inputTypeFor = field => {
-  if (field.field_type === 'date') return 'date';
+  if (field.field_type === 'date') return 'text';
   if (field.field_type === 'number') return 'number';
   return 'text';
 };
@@ -490,6 +530,21 @@ const setFieldValue = (field, value) => {
   missingRequiredKeys.value = missingRequiredKeys.value.filter(key => key !== field.key);
 };
 
+const inputValueForField = field => {
+  const value = valueForField(field);
+  return field.field_type === 'date' ? formatDateInputValue(value) : value;
+};
+
+const setFieldValueFromInput = (field, value) => {
+  const nextValue = field.field_type === 'date' ? normalizeDateInputValue(value) : value;
+  setFieldValue(field, nextValue);
+};
+
+const handleFieldInput = (field, value) => {
+  if (field.field_type === 'date') return;
+  setFieldValueFromInput(field, value);
+};
+
 const fieldOptions = field => {
   return Array.isArray(field.options) ? field.options : [];
 };
@@ -1141,12 +1196,15 @@ onUnmounted(() => {
 
                       <input
                         v-else
-                        :value="valueForField(field)"
+                        :value="inputValueForField(field)"
                         :class="[compactFieldClass, { 'border-n-ruby-8': isFieldMissing(field) }]"
+                        :inputmode="field.field_type === 'date' ? 'numeric' : undefined"
                         :min="field.field_type === 'number' ? 0 : undefined"
+                        :placeholder="field.field_type === 'date' ? DATE_INPUT_PLACEHOLDER : undefined"
                         :step="field.field_type === 'number' ? '0.01' : undefined"
                         :type="inputTypeFor(field)"
-                        @input="setFieldValue(field, $event.target.value)"
+                        @input="handleFieldInput(field, $event.target.value)"
+                        @change="setFieldValueFromInput(field, $event.target.value)"
                       />
                     </div>
                   </div>
diff --git a/app/javascript/dashboard/routes/dashboard/crm/MarketingAnalytics.vue b/app/javascript/dashboard/routes/dashboard/crm/MarketingAnalytics.vue
index 290ce6c..4c2b00e 100644
--- a/app/javascript/dashboard/routes/dashboard/crm/MarketingAnalytics.vue
+++ b/app/javascript/dashboard/routes/dashboard/crm/MarketingAnalytics.vue
@@ -48,6 +48,46 @@ const formatPercent = value => {
   return `${parsed.toFixed(parsed >= 10 ? 0 : 1)}%`;
 };
 
+const DATE_INPUT_PLACEHOLDER = 'dd/mm/yyyy';
+const ISO_DATE_PATTERN = /^(\d{4})-(\d{2})-(\d{2})$/;
+const DISPLAY_DATE_PATTERN = /^(\d{1,2})[./-](\d{1,2})[./-](\d{4})$/;
+
+const padDatePart = value => String(value).padStart(2, '0');
+
+const formatDateInputValue = value => {
+  if (!value) return '';
+
+  const rawValue = String(value).trim();
+  const isoMatch = rawValue.match(ISO_DATE_PATTERN);
+  if (isoMatch) return `${isoMatch[3]}/${isoMatch[2]}/${isoMatch[1]}`;
+
+  return rawValue;
+};
+
+const normalizeDateInputValue = value => {
+  const rawValue = String(value || '').trim();
+  if (!rawValue) return '';
+  if (ISO_DATE_PATTERN.test(rawValue)) return rawValue;
+
+  const displayMatch = rawValue.match(DISPLAY_DATE_PATTERN);
+  if (!displayMatch) return '';
+
+  const day = Number(displayMatch[1]);
+  const month = Number(displayMatch[2]);
+  const year = Number(displayMatch[3]);
+  const date = new Date(Date.UTC(year, month - 1, day));
+
+  if (
+    date.getUTCFullYear() !== year ||
+    date.getUTCMonth() !== month - 1 ||
+    date.getUTCDate() !== day
+  ) {
+    return '';
+  }
+
+  return `${year}-${padDatePart(month)}-${padDatePart(day)}`;
+};
+
 const orderedStages = computed(() => {
   return [...stages.value].sort((a, b) => Number(a.position || 0) - Number(b.position || 0));
 });
@@ -308,8 +348,8 @@ const selectRange = value => {
 };
 
 const updateCustomDates = ({ from = customDateFrom.value, to = customDateTo.value } = {}) => {
-  customDateFrom.value = from;
-  customDateTo.value = to;
+  customDateFrom.value = normalizeDateInputValue(from);
+  customDateTo.value = normalizeDateInputValue(to);
   selectedRange.value = '';
   fetchAnalytics();
 };
@@ -356,18 +396,22 @@ onMounted(fetchAnalytics);
             <label class="grid shrink-0 text-[11px] font-medium text-n-slate-10">
               <span class="sr-only">Date from</span>
               <input
-                :value="customDateFrom"
+                :value="formatDateInputValue(customDateFrom)"
                 class="h-9 w-32 min-w-0 rounded-md border border-n-weak bg-n-background px-3 text-xs text-n-slate-12 outline-none transition focus:border-n-brand"
-                type="date"
+                inputmode="numeric"
+                :placeholder="DATE_INPUT_PLACEHOLDER"
+                type="text"
                 @change="updateCustomDates({ from: $event.target.value, to: customDateTo })"
               />
             </label>
             <label class="grid shrink-0 text-[11px] font-medium text-n-slate-10">
               <span class="sr-only">Date to</span>
               <input
-                :value="customDateTo"
+                :value="formatDateInputValue(customDateTo)"
                 class="h-9 w-32 min-w-0 rounded-md border border-n-weak bg-n-background px-3 text-xs text-n-slate-12 outline-none transition focus:border-n-brand"
-                type="date"
+                inputmode="numeric"
+                :placeholder="DATE_INPUT_PLACEHOLDER"
+                type="text"
                 @change="updateCustomDates({ from: customDateFrom, to: $event.target.value })"
               />
             </label>
